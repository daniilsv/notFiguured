part of 'theme.dart';

class AppBorderRadius {
  static const textField = BorderRadius.all(Radius.circular(8));
  static const card = BorderRadius.all(Radius.circular(28));
  static const smallCard = BorderRadius.all(Radius.circular(15));
  static const historyCard = BorderRadius.all(Radius.circular(8));
  static const button = BorderRadius.all(Radius.circular(12));
  static const calculatorCard = BorderRadius.all(Radius.circular(20));
  static const badge = BorderRadius.all(Radius.circular(20));
  static const c4 = BorderRadius.all(Radius.circular(4));
  static const chartColumn = BorderRadius.vertical(top: Radius.circular(3));
}
